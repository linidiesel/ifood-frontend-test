const STATUS = {
    USER_UNAUTHORIZED: 'user unauthorized',
    USER_AUTHORIZED: 'user authorized'
}

const SPOTIFY = {
    ACCESS_DENIED: 'error',
    ACCESS_ALLOWED: 'code'
};

export { STATUS, SPOTIFY };
